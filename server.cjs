const http=require('http'),fs=require('fs'),path=require('path');const port=process.env.PORT||8775;http.createServer((req,res)=>{let p=path.join(__dirname,req.url==='/'?'index.html':req.url);fs.readFile(p,(e,d)=>{if(e){res.writeHead(404);res.end('not found');return}res.writeHead(200,{'Content-Type':p.endsWith('.html')?'text/html; charset=utf-8':'text/plain; charset=utf-8'});res.end(d)})}).listen(port,'127.0.0.1',()=>console.log('preview http://127.0.0.1:'+port));